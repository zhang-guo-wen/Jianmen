# 数据库账号测试连接统一语义设计

日期：2026-06-30

## 背景

数据库管理里存在多处“测试连接/探测连接”入口，用户期望它们语义一致，但凭据来源按入口区分：连接弹窗和编辑打开时使用已保存凭据；新增/编辑表单里的测试连接使用用户当前输入的账号密码。

## 目标

统一数据库账号测试连接行为：

1. 新增账号弹窗提供“测试连接”按钮，使用表单中输入的数据库用户名和密码。
2. 编辑账号弹窗打开时自动探测一次，使用数据库中已保存的账号密码。
3. 编辑账号弹窗点击“测试连接”时，必须要求用户重新输入数据库密码，并使用表单中输入的用户名和密码。
4. 连接弹窗保持现有行为：打开时自动探测，使用数据库中已保存的账号密码。

## 后端接口

保留现有接口：

- `POST /api/db/accounts/test/{id}`
- 使用已保存账号密码，适用于连接弹窗和编辑弹窗打开时自动探测。

新增临时测试接口：

- `POST /api/db/accounts/test`
- 请求体：

```json
{
  "instance_id": "db-instance-id",
  "username": "user_dba",
  "password": "secret"
}
```

- 不保存密码，不回显密码。
- 返回格式沿用现有 `{ ok, error, latency_ms }`。

## 前端行为

在 `DatabaseView.vue` 的账号新增/编辑弹窗中增加测试连接区域：

- 按钮：`测试连接`
- 状态：测试中 / 可达 / 不可达 / 延迟 / 错误信息
- 新增账号：用户名、密码必填后才能测试。
- 编辑账号：
  - 打开弹窗时自动调用已保存凭据测试接口。
  - 点击按钮时要求密码必填，使用当前输入用户名和密码临时测试。
  - 保存账号时仍保留原语义：密码留空表示保留旧密码。

## 测试

后端增加测试覆盖临时测试接口：

- 缺少 `instance_id`、`username`、`password` 时返回 400。
- 临时测试不保存数据库账号。
- 现有按账号 ID 测试接口继续使用已保存账号。

前端通过 `npm run typecheck` 和 `npm run build` 验证类型与构建。
