# Jianmen 工作项台账

最后更新：2026-07-19

本文件用于统一管理需求、缺陷、安全问题、技术债和测试/运维改进，是当前工作项状态的唯一入口。历史审计和实施计划只作为证据，不直接代表当前状态；历史记录与本文件冲突时，以本文件最近一次复核结果为准。

## 1. 管理原则

1. 新发现的问题先登记、分级和确认范围，未经确认不得顺手实现。
2. P0/P1 可中断当前计划；P2 按当前切片的完成标准处理；P3 默认进入 backlog。
3. 每个切片最多一轮实现、一轮独立复审和一轮修复。仍有争议时拆成新工作项，不循环扩大范围。
4. 一个工作项对应一个可独立验收的小提交；禁止长期维持大规模未提交工作区。
5. 开发期间只运行目标包测试。全仓测试、Docker 实库测试和完整打包只在阶段提交及最终合并前执行。
6. 合并回 `dev` 前必须先合并最新 `dev`，解决冲突并完成适用于本次变更的门禁。
7. 完成、延期和取消都必须记录原因及证据，不能只删除工作项。

## 2. 类型与编号

| 前缀 | 类型 | 使用场景 |
|---|---|---|
| `REQ` | 需求 | 新的用户价值、业务能力或产品行为 |
| `DEF` | 缺陷 | 已有承诺行为错误、退化或不可用 |
| `SEC` | 安全问题 | 权限、认证、敏感数据、协议边界等安全风险 |
| `TECH` | 技术债 | 架构、可维护性、性能或迁移治理问题 |
| `TEST` | 测试改进 | 自动化覆盖、测试夹具或质量门禁缺口 |
| `OPS` | 工程运维 | 构建、发布、启动、监控和运维体验问题 |

编号格式为 `<类型>-<YYYYMMDD>-<三位序号>`。编号创建后不得复用。

## 3. 优先级

| 优先级 | 定义 | 处理要求 |
|---|---|---|
| P0 | 可直接导致 RCE、大范围权限绕过、不可逆数据损坏、核心系统完全不可用 | 立即停止其他工作，修复并独立复审 |
| P1 | 敏感数据泄露、有限范围权限绕过/数据损坏、核心链路无可接受替代方案 | 当前阶段阻断项，优先修复 |
| P2 | 重要功能或工程可靠性问题，有临时替代方案且无直接高危安全影响 | 当前切片内则清零，否则排入明确阶段 |
| P3 | 非关键增强、代码整洁度、穷举测试、体验或性能优化 | 默认延期，不阻断阶段交付 |

优先级按影响而不是实现难度确定。无法复现或证据不足的历史问题使用“待复核”状态，不直接按旧优先级阻断开发。

## 4. 状态流转

正常流程：

```text
待分诊 -> 待复核 -> 已确认 -> 待开发 -> 开发中 -> 复审中 -> 待验证 -> 已完成
```

旁路状态：

- `已阻塞`：外部依赖或必要决策未满足，必须写明阻塞条件。
- `已延期`：问题有效，但不进入当前阶段，必须写明重新评审时间或触发条件。
- `已取消`：不再符合产品方向、已被替代或收益不足，必须保留决策依据。

同一工作项只能有一个当前状态。开始开发前必须从“已确认”进入“待开发”，禁止直接从“待分诊”跳到“开发中”。

## 5. 工作项最小字段

每个工作项至少记录：

| 字段 | 要求 |
|---|---|
| ID / 标题 | 唯一编号和面向结果的简短标题 |
| 类型 / 优先级 / 状态 | 使用本文定义的枚举 |
| 所属阶段 | 当前阶段、下阶段或 backlog |
| 负责人 | 未分配时写“待分配” |
| 来源 | 用户反馈、审计、测试、日志或关联文档 |
| 影响范围 | 用户、资源类型、协议、数据或部署方式 |
| 当前行为 | 已观察到的事实，不写猜测 |
| 目标行为 | 修复或交付后应达到的结果 |
| 验收标准 | 可执行、可判定，至少包含正常路径和失败路径 |
| 验证证据 | 测试命令、结果、提交号或截图/日志位置 |
| 决策记录 | 延期、取消、拆分或优先级变化的原因 |

### 需求补充字段

- 用户价值和目标用户。
- 范围内与范围外内容。
- API、权限、审计、数据模型和连接路径影响。
- 交互或配置变化。

### 缺陷补充字段

- 复现前提和最小复现步骤。
- 实际结果与预期结果。
- 首次发现版本/提交。
- 回归风险和需要补充的自动化测试。

## 6. Ready 与 Done

### Definition of Ready

工作项进入“待开发”前必须满足：

- 影响和优先级已确认；
- 范围、非目标和验收标准明确；
- 依赖、数据迁移及安全影响已识别；
- 能拆成一个小提交，或已经拆成多个独立工作项；
- 明确目标测试，不要求开发过程反复运行全仓门禁。

### Definition of Done

工作项进入“已完成”前必须满足：

- 验收标准全部通过；
- 目标测试通过，错误路径有覆盖；
- 完成一轮独立复审及至多一轮修复；
- 没有把新发现的非 P0/P1 问题混入提交；
- 提交已记录，文档状态和验证证据已更新；
- 阶段合并时已合并最新 `dev` 并执行仓库要求的完整门禁。

## 7. 当前快照

### 本阶段必须完成

数据库网关前一阶段已经完成；本次速度优先阶段完成 `DEF-20260719-004`、`TECH-20260719-003`，以及 `TECH-20260719-001`/`TECH-20260719-004` 的用户会话、连接密码签发、应用、容器端点、平台账号、主机/账号和数据库实例/账号切片，这些已实施切片的 P0/P1/P2 已清零。全局仍保留 18 项 Backlog（P2=2、P3=16），均列在下方，未经确认不进入本阶段实现。以下已验证修复不得因历史计划未勾选而重新列为开放问题：

| 事项 | 结果 | 提交 | 验证证据 |
|---|---|---|---|
| 旧数据库审计接口缺少 RBAC | 已完成 | `db27381` | [RBAC 回归测试](../internal/server/admin/legacy_audit_rbac_test.go) |
| 审计会话创建失败时仍允许转发 | 已完成 | `ebb3bd7` | [失败关闭测试](../internal/server/dbproxy/server_audit_gate_test.go) |
| 大响应被完整缓冲 | 已完成 | `4f97f9b`、`64b311a` | [目标测试](../internal/server/dbproxy/observer_large_response_test.go)、[实库集成测试](../internal/integration/database_proxy_integration_test.go) |
| 数据库审计 SQL 脱敏断言 | 已完成 | `2caaa32` | [实库集成测试](../internal/integration/database_proxy_integration_test.go) |
| 审计保留、分批清理与回放字节配额 | 已完成 | `8b8b633`、`99cf3aa` | [保留服务测试](../internal/service/audit_retention_test.go)、[存储一致性测试](../internal/store/dbstore_audit_retention_test.go) |
| 跨分片脱敏及审计录制失败关闭 | 已完成 | `c517a3b` | [流式脱敏测试](../internal/recording/stream_redactor_test.go)、[数据库录制失败测试](../internal/server/dbproxy/audit_replay_path_test.go) |
| 超级管理员独立文件权威路径与无管理员锁死 | 已完成 | `15e9c28`、`1c1b141` | 启动不再读取或重命名 `.super_admin_ids`；有用户但无有效超级管理员时失败关闭；显式配置只允许在无有效管理员时写库恢复；[启动回归测试](../cmd/bastion-core/bootstrap_test.go)、[存储事务测试](../internal/storage/super_admin_test.go) |
| SSH 对聚合 Store 的直接依赖 | 已完成，阶段门禁通过 | `a3e145a` | SSH 使用方定义认证、目标、用户会话、审计会话和审计事件 5 组窄接口，共 9 个方法；[最小依赖回归测试](../internal/server/sshserver/repository_test.go) |
| `TEST-20260719-001` 数据库真实协议兼容矩阵与模糊测试 | 已完成 | 本次提交 | [兼容矩阵](./database-protocol-compatibility.md)、[MySQL 实库](../internal/integration/mysql_proxy_integration_test.go)、[PostgreSQL 实库](../internal/integration/postgres_proxy_integration_test.go)、[Redis 实库](../internal/integration/redis_proxy_integration_test.go)、[监听生命周期](../internal/server/dbproxy/listeners_test.go) |
| `TEST-20260719-002` Redis Relay 并发边界 | 已完成，阶段门禁通过 | `5dd41e0` | Relay 入口幂等强制串行化 Observer；原始 Redis Drain 竞态用例、全 `dbproxy` 包及组合 Linux/CGO Race 测试通过；无 P0/P1/P2 |
| `DEF-20260719-001` 启动烟测绕过正式登录 | 已完成，主目录运行烟测通过 | `7c80f1a`、`8402830` | `start.ps1` 使用正式 ALTCHA challenge、统一响应封装和浏览器会话；失败硬退出；不再跳过 Admin/前端代理检查或输出 Token；主目录启动返回 0，正式登录成功，Admin/Web/SSH/数据库端口、前端与 API 代理验证通过 |
| Admin 非 Server 聚合 Store 依赖 | 已完成，阶段门禁通过 | `1f8acb9` | Web Terminal 审计改为 3 方法使用方接口；角色服务 helper 直接依赖 `service.RoleManagementRepository`；目标包及全仓测试通过 |
| `TECH-20260719-002` 拆分并删除 76 方法 `store.Store` | 已完成，阶段门禁通过 | `133ff7a`、`61f1480` | Admin 按资源域持有使用方窄接口，构造参数恢复编译期约束并拒绝 typed-nil；全仓已删除聚合接口；[边界与生产构造测试](../internal/server/admin/repository_test.go)、全仓测试及 Linux Race 通过 |
| `DEF-20260719-002` 长期 `started` 审计会话 | 已完成，阶段门禁通过 | `540d282`、`db5dbab` | SSH、Web Terminal、数据库和 Web RDP 会话统一获得进程租约；活动会话分批心跳，行数不一致失败关闭；仅完整且过期的租约可恢复；结束和 RDP 完成按 owner fencing，晚到进程不能覆盖租约过期证据；[租约并发测试](../internal/store/dbstore_audit_lease_test.go)、[版本化迁移测试](../internal/storage/migration_audit_session_lease_test.go)、[MySQL/PostgreSQL 实库迁移](../internal/integration/database_storage_migration_integration_test.go)通过 |
| `OPS-20260719-001` Linux Node 24 锁文件不完整 | 已完成，容器门禁通过 | `9070af0` | `node:24-bookworm-slim` 曾因缺少 `@emnapi/core/runtime@1.11.2` 锁记录拒绝 `npm ci`；按同一镜像 npm 仅重建锁元数据，Linux 容器完整构建、Windows `npm ci` 与类型检查通过；独立复审无 P0/P1/P2 |
| `DEF-20260719-003` Web RDP 审计对象索引超过 MySQL 限制 | 已完成，实库矩阵通过 | `fa9f688` | `audit_artifacts.object_key` 从超出 `utf8mb4` 3072 字节索引上限的 1024 字符收敛为 255 字符，保留完整唯一约束；MySQL 8 目标迁移、Docker 实库全矩阵、全仓测试、Linux Race、完整打包及容器构建通过；独立复审无 P0/P1/P2 |
| `DEF-20260719-004` Vue 连接权限属性命名不一致 | 已完成，组件权限组合已挂载验证 | `d20e15e`、`c0b011f`、`2011c77` | prop 统一为 `allowSsh`/`allowSftp`，调用方继续使用 Vue 模板的 `allow-ssh`/`allow-sftp`；[真实挂载测试](../web/src/components/ConnectionConfigDialog.mount.test.ts)覆盖四种 SSH/SFTP 权限组合，[命令与静态契约测试](../web/src/components/ConnectionConfigDialog.test.ts)通过 |
| `TECH-20260719-003` 授权列表真实批量化 | 已完成，查询次数不随资源数线性增长 | `ffd4526`、`97a96a8`、`3509ce9` | 批量授权下沉到 Repository/SQL，按请求索引隔离资源与动作语义；平台账号、父资源组、账号组、直接/用户组/临时授权及全局拒绝优先级均有回归；[授权服务测试](../internal/service/authorization_test.go)、[RBAC 测试](../internal/rbac/checker_test.go)通过 |
| `TECH-20260719-001`/`TECH-20260719-004` 用户会话切片 | 当前切片已完成，全局分层债务继续保留 | `f229f6f`、`ac5afcb`、`dbd1853` | 普通与 AI 会话统一经 Service/Repository 创建永久会话；同一用户并发复用、不同用户序列唯一，事务锁覆盖 MySQL 8/PostgreSQL 16；[服务测试](../internal/service/user_session_creation_test.go)、[实库并发测试](../internal/integration/user_session_atomicity_integration_test.go)通过 |
| `TECH-20260719-001`/`TECH-20260719-004` 连接密码签发切片 | 当前切片已完成，全局分层债务继续保留 | `cc8a7da` | 普通与 AI 入口统一经 `ConnectionPasswordService`、使用方 Repository 和 Authorizer 签发；Handler 不再执行 SQL；请求 Context 贯穿查询、授权和持久化；只持久化哈希且明文仅返回一次；目标包与 Linux/CGO Race 通过，独立安全复审无 P0/P1/P2 |
| `TECH-20260719-004` 应用、容器端点与平台账号 Context 切片 | 三个并行切片已完成，全局 Context 债务继续保留 | `a77c3b8`、`5e8edac`、`aee3fb0`、`60ae210` | 15 个历史 Repository 方法改为 `context.Context` 首参并贯穿 GORM 查询和事务；取消写入回归证明无部分资源同步。平台账号独立安全复审发现“取消阻止补偿”和“提交后回填”两项 P2，唯一修复轮将回填纳入事务并使用有界独立补偿 Context，切片 P2 已清零；三个目标包测试均通过 |
| `TECH-20260719-004` 主机/账号与数据库实例/账号 Context 切片 | 两个并行切片已完成，全局 Context 债务继续保留 | `48c4830`、`f2b94c5`、`013449e`、`0e84117` | Host/Target 11 个、Database 10 个历史 Repository 方法改为 `context.Context` 首参并贯穿查询、锁和事务。独立复审发现账号跨主机变更 P1，以及取消补偿、创建原子性和协议竞态等 P2；唯一修复轮完成同主机事务锁、跨主机拒绝、有界独立补偿和事务化创建。数据库切片同时修复无界清理 P2；目标包测试与集成测试编译均通过，两个切片 P0/P1/P2 已清零 |
| 数据库配置随机源并发访问 | Linux Race 门禁发现后已修复 | `e0f0be5` | 服务统一串行访问可注入随机源；原并发反配置测试连续 20 轮及 `rbac/service/store/admin/cmd` 组合 Linux/CGO `-race` 通过；[并发回归测试](../internal/service/db_deprovision_test.go) |

阶段门禁证据（2026-07-19）：

- 后端：`go build ./...`、`go test ./... -count=1`、`go vet ./...` 全部通过。
- 前端：`npm run typecheck`、`npm run build` 全部通过；真实挂载测试 4/4、命令与静态契约测试 39/39。
- 实库：`JIANMEN_REQUIRE_DOCKER=1 go test -tags=integration ./internal/integration -count=1 -timeout=30m` 通过，耗时 631.07 秒。
- 并发：Linux/CGO `-race` 组合包通过；数据库配置随机源原竞态用例连续 20 轮通过。
- 打包：`.\build.ps1` 通过，产出 Windows amd64 37.7 MB、Linux amd64 36.9 MB。
- 容器：生产 Dockerfile 在线重建被外部 Alpine 镜像源临时 DNS 错误阻塞，未修改 Dockerfile；使用本地已验证 runtime 镜像和本次精确 Linux 产物构建离线镜像成功，网络隔离启动后 `/api/init/status` 返回成功，进程以 UID/GID 10001 运行。在线重建属于外部环境阻塞，不改变本阶段代码验收结论。

### 可以延期

| ID | 优先级 | 状态 | 工作项 | 验收摘要 | 复评时间/触发条件 | 来源 |
|---|---|---|---|---|---|---|
| `TECH-20260719-001` | P2 | 已延期（局部完成） | 用户会话创建和连接密码签发路径已迁入 `handler -> service -> store`；其余 `internal/server/admin` 资源域的 Handler、业务判断和持久层访问尚未完全分层 | 后续仅按单一资源域继续迁移；Handler 不直接执行 SQL；目标包测试通过 | 修改对应资源行为或文件接近行数上限时 | [后端审计](./backend-audit-2026-07-18.md) |
| `TECH-20260719-004` | P2 | 已延期（局部完成） | 用户会话、连接密码、应用、容器端点、平台账号、主机/账号和数据库实例/账号 Repository 已使用请求 context；审计等历史方法仍未以 `context.Context` 为首参数，取消和超时尚未全量贯穿数据库 | 后续仅按单一资源域迁移 Handler、Service 和 Repository；取消/超时测试证明无后台遗留查询 | 修改对应资源域，或出现取消/超时传播缺陷时 | `TECH-20260719-002` 关闭决策 |
| `TECH-20260719-005` | P3 | 已延期 | DTO、GORM Model 和 Store View 类型仍未彻底分离 | Model 不承载 API JSON 结构；Store 不堆放表现层 View；迁移不改变 API 行为 | 修改关联 API 或类型再次造成跨层依赖时 | [后端审计](./backend-audit-2026-07-18.md) |
| `TECH-20260719-006` | P3 | 已延期 | 前端生产构建仍有第三方 PURE 注释告警和主分包体积告警 | 先确认无功能风险；按页面拆分主包并记录体积基线；不为消除第三方告警修改依赖源码 | 主包继续增长或进入发布性能优化阶段时 | 最近一次阶段构建输出 |
| `TECH-20260719-007` | P3 | 已延期 | 回放配额统计当前每小时遍历回放根目录，超大规模下耗时随文件数线性增长 | 记录会话关闭时的回放字节数并增量维护总量；以容量基准证明清理周期稳定 | 回放文件超过 100 万或扫描超过清理周期的 10% 时 | 审计治理切片复核 |
| `TEST-20260719-003` | P3 | 已延期 | Admin 生产构造、静态接口组合和 typed-nil 已覆盖；Web Terminal 审计底层报错传播仍缺少显式回归断言 | 在不扩大生产代码的前提下补 Web Terminal 失败路径断言；保持原错误语义和失败关闭行为 | 下次修改 Web Terminal 审计适配器时 | `1f8acb9` 独立复审、`61f1480` 修复证据 |
| `OPS-20260719-002` | P3 | 已延期 | 测试依赖链仍输出 `glob@10.5.0` 弃用警告；当前 `npm audit` 为 0 漏洞且不影响生产包 | 随 Vitest/happy-dom 常规升级消除传递依赖，不单独修改锁文件或替换生产依赖 | 测试框架升级，或该依赖出现安全公告时 | 本阶段 `npm ci` 输出 |
| `TEST-20260719-004` | P3 | 已延期 | 连接密码核心禁用、过期、资源类型校验和错误脱敏已有 Service 单测，但普通与 AI Handler 尚无逐项端到端回归断言 | 仅补 Handler 级回归，不改变生产行为；覆盖两个入口的失败状态和固定错误响应 | 下次修改连接密码 Handler 或统一 API 测试基建时 | `cc8a7da` 独立安全复审 |
| `TECH-20260719-008` | P3 | 已延期 | 普通连接密码入口仅接收 `target_id`；不同资源类型人为制造同 ID 时沿用“主机账号优先”解析，当前授权仍按最终资源失败关闭 | 后续接口显式接收并校验 `resource_type`，消除语义歧义；不作为当前权限缺陷处理 | 修改连接密码请求契约时 | `cc8a7da` 独立安全复审 |
| `TECH-20260719-009` | P3 | 已延期 | `Applications` 与 `DatabaseInstances` 历史签名只返回切片，数据库错误或 Context 取消会折叠为空列表，虽查询已停止但 HTTP 错误语义较弱 | 两个列表方法均改为显式返回 error，由 Handler 区分空数据与查询失败 | 下次修改应用或数据库实例列表契约时 | `a77c3b8`、`013449e` 独立复审 |
| `TEST-20260719-005` | P3 | 已延期 | 容器端点取消回归已覆盖读取和新增，尚未逐项覆盖更新、删除及 runtime 超时路径 | 补目标测试证明更新/删除取消不落库，runtime 保持 20 秒有界 Context | 下次修改容器端点持久层或 runtime 调用时 | `5e8edac` 独立复审 |
| `TEST-20260719-006` | P3 | 已延期 | 平台账号密码读取取消测试只断言返回错误，未显式断言 `errors.Is(err, context.Canceled)` | 收紧错误链断言，不改变生产代码或对外响应 | 下次修改平台账号存储测试时 | `aee3fb0` 独立安全复审 |
| `TEST-20260719-007` | P3 | 已延期 | 平台账号列表和详情 DTO 当前不含密码，密码读取仍受 `use` 授权，但缺少 Handler 级“密码不进入列表、详情、错误和日志”的穷举回归 | 仅补脱敏与授权回归，禁止在测试输出中打印真实密码 | 下次修改平台账号 Handler 或统一脱敏测试基建时 | `aee3fb0` 独立安全复审 |
| `TEST-20260719-008` | P3 | 已延期 | 数据库资源取消回归已覆盖新增，尚未逐项覆盖更新、删除及事务执行中途取消 | 补目标测试证明更新/删除与中途取消不会留下部分数据库实例、账号或授权资源 | 下次修改数据库资源持久层时 | `013449e`、`0e84117` 独立复审 |
| `TECH-20260719-010` | P3 | 已延期 | 数据库实例或账号创建后续步骤失败时，有界补偿会删除已建资源，但自动创建的空资源组可能保留；不影响授权边界或业务数据完整性 | 后续资源组生命周期重构时，将空组回收纳入同一事务或显式补偿，并增加回归断言 | 修改数据库资源组创建或清理规则时 | `0e84117` 独立复审 |
| `TECH-20260719-011` | P3 | 已延期 | `TargetConfig` 及部分主机/账号更新删除路径可能把 Context 取消包装成 NotFound；当前仍失败关闭，但错误语义不准确 | 保留 `context.Canceled`/`DeadlineExceeded` 错误链，仅在真实无记录时返回 NotFound | 下次修改主机/账号错误映射契约时 | `48c4830`、`f2b94c5` 独立安全复审 |
| `TEST-20260719-009` | P3 | 已延期 | 主机/账号跨主机拒绝与创建补偿已有回归，但更新、删除及事务执行中途取消仍缺少逐项覆盖 | 补目标测试证明更新/删除取消不落库，并覆盖事务锁持有期间的中途取消 | 下次修改主机/账号持久层时 | `f2b94c5` 独立安全复审 |
| `TECH-20260719-012` | P3 | 已延期 | Admin 主机/账号列表失败时仍可能把底层数据库错误文本直接返回客户端，存在内部实现信息暴露风险 | 对外返回固定错误码和脱敏消息，完整错误仅进入受控日志；保持失败关闭 | 下次统一 Admin 错误响应或修改主机列表 Handler 时 | `48c4830`、`f2b94c5` 独立安全复审 |

### 建议取消或关闭

| 原事项 | 建议 | 原因 / 替代证据 |
|---|---|---|
| 将整个 Phase 2 计划作为一次连续重构执行 | 取消整体执行方式 | 计划已过期且部分事项已完成；剩余内容应按上表重新分片，不再形成大型工作区 |
| 临时授权事务化、用户/用户组与 RBAC 服务化、生产默认版本化迁移 | 关闭历史待办 | 已由 `415512d`、`08991e8`、`fbcdc35` 等后续提交覆盖；如发现回归应新建 `DEF`，不复用旧任务 |
| 立即继续拆分 `observer.go` 和 `mysql_auth.go` | 取消当前排期 | 当前分别约 348 行和 453 行，低于代理会话文件 600 行上限，且近期已有协议加固重构 |
| 未经重新确认直接引入“稳定资源分组成员关系”新模型 | 取消旧方案 | 资源分组产品模型已发生变化；若仍有授权一致性缺陷，应先以可复现行为重新定义需求 |

## 8. 新工作项模板

复制以下模板新增工作项：

```markdown
### <ID> <标题>

- 类型：
- 优先级：
- 状态：待分诊
- 所属阶段：backlog
- 负责人：待分配
- 来源：
- 创建/更新日期：

#### 影响与现状

- 影响范围：
- 当前行为：
- 复现/证据：

#### 目标与范围

- 目标行为：
- 范围内：
- 范围外：

#### 验收标准

- [ ] 正常路径：
- [ ] 失败路径：
- [ ] 权限、审计和敏感数据检查：
- [ ] 目标测试：

#### 决策与验证

- 决策记录：
- 提交：
- 验证证据：
```

## 9. 阶段汇报格式

每次阶段汇报只更新以下四项：

```text
已完成并提交：
当前阻塞项：
剩余任务数量：P0 / P1 / P2 / P3
下一阶段预计完成：
```

新增计划外工作必须先登记 ID、分级并获得确认。
