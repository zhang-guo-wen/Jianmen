# Jianmen 未完成工作项

最后更新：2026-07-21

本文件只保留尚未完成的工作项。已完成、已取消和阶段验证记录通过 Git 历史与 `docs/archive/` 查询。当前共有 41 项，均为 P3 延期事项。

| ID | 优先级 | 状态 | 工作项 | 验收摘要 | 复评时间/触发条件 | 来源 |
|---|---|---|---|---|---|---|
| `TECH-20260719-005` | P3 | 已延期 | DTO、GORM Model 和 Store View 类型仍未彻底分离 | Model 不承载 API JSON 结构；Store 不堆放表现层 View；迁移不改变 API 行为 | 修改关联 API 或类型再次造成跨层依赖时 | [后端审计](./reviews/backend-audit-2026-07-18.md) |
| `TECH-20260719-006` | P3 | 已延期 | 前端生产构建仍有第三方 PURE 注释告警和主分包体积告警、SetupView 遗留 CSS | 先确认无功能风险；按页面拆分主包并记录体积基线；不为消除第三方告警修改依赖源码 | 主包继续增长或进入发布性能优化阶段时 | 最近一次阶段构建输出 |
| `TECH-20260719-007` | P3 | 已延期 | 回放配额统计当前每小时遍历回放根目录，超大规模下耗时随文件数线性增长 | 记录会话关闭时的回放字节数并增量维护总量；以容量基准证明清理周期稳定 | 回放文件超过 100 万或扫描超过清理周期的 10% 时 | 审计治理切片复核 |
| `TEST-20260719-003` | P3 | 已延期 | Admin 生产构造、静态接口组合和 typed-nil 已覆盖；SSH/Web Terminal 命令与 SFTP 审计写入失败后的 fatal 和双端关闭仍缺少协议级集成断言 | 在不扩大生产代码的前提下补失败路径断言；保持原错误语义和失败关闭行为 | 下次修改 SSH/Web Terminal 审计适配器时 | `1f8acb9`、`61f1480`、`8bd1941` 独立复审 |
| `OPS-20260719-002` | P3 | 已延期 | 测试依赖链仍输出 `glob@10.5.0` 弃用警告；当前 `npm audit` 为 0 漏洞且不影响生产包 | 随 Vitest/happy-dom 常规升级消除传递依赖，不单独修改锁文件或替换生产依赖 | 测试框架升级，或该依赖出现安全公告时 | 本阶段 `npm ci` 输出 |
| `TEST-20260719-004` | P3 | 已延期 | 连接密码核心禁用、过期、资源类型校验和错误脱敏已有 Service 单测，但普通与 AI Handler 尚无逐项端到端回归断言 | 仅补 Handler 级回归，不改变生产行为；覆盖两个入口的失败状态和固定错误响应 | 下次修改连接密码 Handler 或统一 API 测试基建时 | `cc8a7da` 独立安全复审 |
| `TECH-20260719-008` | P3 | 已延期 | 普通连接密码入口仅接收 `target_id`；不同资源类型人为制造同 ID 时沿用“主机账号优先”解析，当前授权仍按最终资源失败关闭 | 后续接口显式接收并校验 `resource_type`，消除语义歧义；不作为当前权限缺陷处理 | 修改连接密码请求契约时 | `cc8a7da` 独立安全复审 |
| `TECH-20260719-009` | P3 | 已延期 | `Applications` 与 `DatabaseInstances` 历史签名只返回切片，数据库错误或 Context 取消会折叠为空列表，虽查询已停止但 HTTP 错误语义较弱 | 两个列表方法均改为显式返回 error，由 Handler 区分空数据与查询失败 | 下次修改应用或数据库实例列表契约时 | `a77c3b8`、`013449e` 独立复审 |
| `TEST-20260719-005` | P3 | 已延期 | 容器端点取消回归已覆盖读取和新增，尚未逐项覆盖更新、删除及 runtime 超时路径，缺少更广数据库与真实数据库矩阵 | 补目标测试证明更新/删除取消不落库，runtime 保持 20 秒有界 Context，并补齐数据库/容器并发矩阵 | 下次修改容器端点持久层或 runtime 调用时 | `5e8edac` 独立复审 |
| `TEST-20260719-006` | P3 | 已延期 | 平台账号密码读取取消测试只断言返回错误，未显式断言 `errors.Is(err, context.Canceled)` | 收紧错误链断言，不改变生产代码或对外响应 | 下次修改平台账号存储测试时 | `aee3fb0` 独立安全复审 |
| `TEST-20260719-007` | P3 | 已延期 | 平台账号列表和详情 DTO 当前不含密码，密码读取仍受 `use` 授权，但缺少 Handler 级“密码不进入列表、详情、错误和日志”的穷举回归 | 仅补脱敏与授权回归，禁止在测试输出中打印真实密码 | 下次修改平台账号 Handler 或统一脱敏测试基建时 | `aee3fb0` 独立安全复审 |
| `TEST-20260719-008` | P3 | 已延期 | 数据库资源取消回归已覆盖新增，尚未逐项覆盖更新、删除及事务执行中途取消 | 补目标测试证明更新/删除与中途取消不会留下部分数据库实例、账号或授权资源，并补齐真实数据库并发矩阵 | 下次修改数据库资源持久层时 | `013449e`、`0e84117` 独立复审 |
| `TECH-20260719-010` | P3 | 已延期 | 数据库实例或账号创建后续步骤失败时，有界补偿会删除已建资源，但自动创建的空资源组可能保留；不影响授权边界或业务数据完整性 | 后续资源组生命周期重构时，将空组回收纳入同一事务或显式补偿，并增加回归断言 | 修改数据库资源组创建或清理规则时 | `0e84117` 独立复审 |
| `TECH-20260719-011` | P3 | 已延期 | `TargetConfig` 及部分主机/账号更新删除路径可能把 Context 取消包装成 NotFound；当前仍失败关闭，但错误语义不准确 | 保留 `context.Canceled`/`DeadlineExceeded` 错误链，仅在真实无记录时返回 NotFound | 下次修改主机/账号错误映射契约时 | `48c4830`、`f2b94c5` 独立安全复审 |
| `TEST-20260719-009` | P3 | 已延期 | 主机/账号跨主机拒绝与创建补偿已有回归，但更新、删除及事务执行中途取消仍缺少逐项覆盖 | 补目标测试证明更新/删除取消不落库，并覆盖事务锁持有期间的中途取消，补齐主机真实数据库矩阵 | 下次修改主机/账号持久层时 | `f2b94c5` 独立安全复审 |
| `TECH-20260719-012` | P3 | 已延期 | Admin 主机/账号列表与原始错误映射失败时仍可能把底层数据库错误文本直接返回客户端，存在内部实现信息暴露风险 | 对外返回固定错误码和脱敏消息，完整错误仅进入受控日志；保持失败关闭 | 下次统一 Admin 错误响应或修改主机列表 Handler 时 | `48c4830`、`f2b94c5` 独立安全复审 |
| `TECH-20260719-013` | P3 | 已延期 | Web RDP 最终化当前使用 5 秒有界 `context.Background()`，不会继承会话 Context values；最终化仍有界且不影响审计完整性，但链路追踪值会丢失 | 改为基于会话 Context 的 `context.WithoutCancel` 加 5 秒超时，保持最终化不受父取消影响 | 下次修改 Web RDP 会话最终化时 | `8bd1941` 实现复核 |
| `TECH-20260720-001` | P3 | 已延期 | Admin 可见性聚合仍有两处窄仓库直连；Host 服务接口仍使用 `config.Target`，旧 visibility/DB helper 与 Store View 边界待清理 | 保持现有权限失败关闭行为，按单一资源域迁移到 Service/DTO，不改变 API 契约 | 下次修改资源可见性、Host 适配器或 Store View 时 | 本阶段最终有界复核 |
| `TEST-20260720-001` | P3 | 已延期 | 认证初始化缺少真实 MySQL/PostgreSQL 并发 claim、时间侧信道/GORM 日志一致性及边界穷举 | 补真实数据库并发矩阵和统一失败响应/日志断言，不修改当前认证协议 | 下次修改初始化 claim、登录失败响应或数据库事务策略时 | `b0e520e` 独立安全复审 |
| `TECH-20260720-002` | P3 | 已延期 | 应用生命周期尚未覆盖异步监听失败、列表错误显式返回、完整补偿 saga 与多实例锁 | 明确跨实例串行化与补偿契约，所有异步失败可观察且不留下孤立监听 | 下次修改应用监听器或引入多实例部署时 | `54fd4aa` 独立复审 |
| `TEST-20260720-002` | P3 | 已延期 | AI 资源缺少 H/D 前缀、nil Context、RDP/未知协议及真实 SQLite 生命周期状态负面矩阵 | 仅补负面与真实状态矩阵，保持未授权、无效状态及未知协议统一失败关闭 | 下次修改 AI 资源标识、协议或生命周期状态时 | `b12612d` 独立安全复审 |
| `TECH-20260720-003` | P3 | 已延期 | AI 资源 `resource_id` 语义尚未统一；Token 轮换提交后再校验存在非安全性的可用性边界 | 统一列表与凭证响应的资源标识，定义轮换后的幂等/恢复语义且不降低授权强度 | 下次修改 AI 资源 API 或 Token 轮换契约时 | `b12612d` 独立安全复审 |
| `TECH-20260720-004` | P3 | 已延期 | 登录与管理操作已使用结构化 `intent_id` 关联 intent/result，列表合并、筛选、分页和总数已按逻辑记录计算；仍缺 pending intent 的自动对账清理，刷新最终审计失败也可能消耗已轮换 Token | 建立可重试对账，不允许补偿路径降低审计失败关闭要求 | 下次修改登录、刷新或审计意图生命周期时 | `internal/store/dbstore_audit_management.go`、`internal/server/admin/audit_query_repository_accuracy_test.go` |
| `TEST-20260720-003` | P3 | 已延期 | 审计查询缺少 URL/真实协议×状态×权限穷举，以及 typed-nil、ResponseWriter 可选接口和 result/panic 覆盖 | 补协议与适配器负面矩阵，证明所有不一致、取消和 panic 路径均失败关闭 | 下次修改审计查询协议识别或 HTTP 包装器时 | `615bfaa` 独立安全复审 |
| `TECH-20260720-005` | P3 | 已延期 | 平台 `expires_at` 缺省/空值、容器字段清空和用户偏好读改写缺少统一契约；并发偏好更新可能丢失，容器错误文本待固定 | 定义 absent/null/empty 语义并使用原子更新或版本控制；固定对外错误且内部详情只入日志 | 下次修改平台账号、容器或偏好更新契约时 | `59f1d33`、`eeddf10`、`b45687a` 独立复审 |
| `TEST-20260720-004` | P3 | 已延期 | 平台账号、容器和用户偏好缺少更广真实数据库/并发矩阵；平台账号测试受 `map` 顺序影响存在已知 flaky | 补稳定排序和真实数据库并发断言，测试不得依赖 map 迭代顺序 | 下次修改对应 Repository 或并发策略时 | `59f1d33`、`eeddf10`、`b45687a` 独立复审 |
| `OPS-20260721-001` | P3 | 已延期 | 超级管理员没有正式的离线恢复入口，当前只能依赖首次向导或配置用户隐式恢复 | 提供仅限本机/容器内执行的恢复命令，可列出超级管理员、重置指定超级管理员密码并恢复启用状态；不经公开 HTTP、不显示旧密码，并记录恢复审计 | 超级管理员密码恢复或移除启动配置用户前 | [现有超级管理员启动保护](../internal/storage/bootstrap.go)、[首次初始化服务](../internal/service/admin_auth.go) |
| `REQ-20260721-001` | P3 | 已延期 | 首次向导只创建超级管理员，不能确认管理后台、SSH 和数据库网关的公开访问地址 | 向导分别采集并校验三个公开地址；监听地址与公开地址保持独立，未填写时给出适合单机零配置部署的默认值 | 开始配置中心或首次向导改造时 | 用户确认的运维简化需求（2026-07-21） |
| `REQ-20260721-002` | P3 | 已延期 | 快速连接命令可能使用监听地址、请求地址或固定本机地址，无法稳定表达 NAT、反向代理后的入口 | 所有 SSH、SFTP 和数据库连接命令统一读取页面权威的公开地址，禁止向用户展示容器内部监听地址 | 实现公开访问地址模型或修改快速连接命令时 | 用户确认的运维简化需求（2026-07-21） |
| `OPS-20260721-002` | P3 | 已延期 | 页面尚不能在保存监听端口前识别占用冲突，只能等重启绑定失败 | 保存候选配置前检测管理后台、SSH、数据库网关和应用端口池冲突；排除当前进程自身已持有的原端口，失败时明确指出冲突地址和端口 | 监听配置迁入页面时 | 用户确认的运维简化需求（2026-07-21） |
| `SEC-20260721-001` | P3 | 已延期 | 网关证书已有启动期验证，但页面保存候选部署证书前没有统一预检 | 保存前验证证书链、目标域名/SAN、有效期、服务端用途及私钥匹配；任何失败均拒绝保存，私钥不得进入响应、日志和审计详情 | TLS 配置迁入页面时 | 用户确认的运维简化需求（2026-07-21） |
| `OPS-20260721-003` | P3 | 已延期 | 已有对象存储手动读写删除诊断，但尚不能对页面中待保存的候选配置自动执行 | 保存对象存储配置前，使用随机临时对象完成写入、读取校验和删除；清理失败需明确告警并拒绝保存，复用现有诊断能力且不重复实现协议逻辑 | 对象存储配置迁入页面时 | [现有对象存储诊断](../internal/service/system_setting_diagnostic.go) |
| `REQ-20260721-003` | P3 | 已延期 | 系统设置只有固定页签，配置增多后难以定位和理解生效状态 | 支持按名称/说明搜索、“只看待重启”筛选，以及基础/高级模式；模式只改变展示范围，不改变配置值或权限 | 系统设置字段扩展时 | 用户确认的运维简化需求（2026-07-21） |
| `OPS-20260721-004` | P3 | 已延期 | 已保存完整配置修订历史，但不能从页面恢复上一版本 | 提供一键恢复上一版本；恢复动作复制旧快照生成新修订，不删除历史，并清楚标记需重启生效 | 配置修订与自动回滚闭环完成后 | [现有系统设置修订服务](../internal/service/system_setting.go) |
| `OPS-20260721-005` | P3 | 已延期 | 当前健康接口只表示进程可响应，首页没有关键依赖和容量状态 | 首页显示磁盘可用空间、录像已用/配额、元数据库、guacd 和对象存储健康状态；单项失败不伪装为整体正常，并提供最近检查时间 | 建设运维首页或统一健康检查时 | 用户确认的运维简化需求（2026-07-21） |
| `SEC-20260721-002` | P3 | 已延期 | 尚无面向运维排障的系统配置导出能力 | 配置导出默认移除密码、Token、私钥、Secret、DSN 凭据和其他敏感值，仅保留“已配置”状态；任何敏感值不得通过可选参数恢复导出 | 增加配置导出或诊断包时 | 用户确认的运维简化需求（2026-07-21） |
| `REQ-20260721-004` | P3 | 已延期 | RDP 安全模式、忽略证书和证书指纹当前保存在主机账号上，并与 Windows 域一起直接铺在新增账号主流程 | 将安全模式、忽略证书和证书指纹迁到 Windows 主机实例；忽略证书默认关闭，账号统一继承实例连接策略。Windows 域仍属于账号登录身份。实例表单的 RDP 安全项与账号表单的 Windows 域分别收进“更多设置”，新增账号主流程只保留用户名和密码等高频项 | 下次修改 Windows 主机/账号模型或新增账号表单时 | [当前账号表单](../web/src/views/HostsView.vue)、[当前主机账号模型](../internal/model/host.go) |
| `REQ-20260721-005` | P3 | 已延期 | Web RDP 复制、粘贴成功后使用全局浮层提示，停留时间较长且会遮挡远程桌面，连续操作还可能堆叠 | 成功反馈改为按钮附近的轻量状态或不遮挡桌面的提示，1 秒内消失且重复操作合并；错误提示保留足够阅读时间并明确权限、浏览器安全上下文等失败原因 | 下次修改 Web RDP 工具栏或统一消息反馈时 | [当前剪贴板反馈](../web/src/views/WebRDPView.vue) |
| `REQ-20260721-006` | P3 | 已延期 | Web RDP 的“下载文件”按钮只弹出一句说明，用户不知道映射盘、上传目标和远端触发下载的具体路径 | 增加清晰的文件传输面板或引导：展示上传/下载/磁盘映射三项权限与可用状态；上传支持选择或拖放并明确文件进入远端“Jianmen”映射盘；下载明确要求在远端把文件放入映射盘的 `Download` 目录，随后由浏览器自动保存；显示进度、结果和失败原因，移除仅弹提示但看似会直接下载的误导交互 | 下次修改 Web RDP 文件通道或工具栏时 | [当前文件传输交互](../web/src/views/WebRDPView.vue)、[Web RDP 权限说明](./guides/web-rdp.md) |
| `REQ-20260721-007` | P3 | 已延期 | “个人设置”和“系统配置”当前作为普通主导航项混在业务菜单中，个人设置页的页签位于左侧 | 在侧边菜单最下方、退出按钮上方增加统一“设置”入口，内含“个人配置”和“系统配置”；个人配置对所有用户可见，系统配置仅超级管理员可见，并从普通业务菜单移除两个独立入口。个人配置页面内部继续使用页签，但统一改为顶部页签并保留可直接访问的路由 | 下次调整应用导航或设置页面信息架构时 | [当前导航](../web/src/navigation.ts)、[当前应用侧边栏](../web/src/App.vue)、[当前个人设置页](../web/src/views/SettingsView.vue) |
| `REQ-20260721-008` | P3 | 已延期 | 本地 SSH/数据库客户端配置把“我已执行以上命令”复选框和“加载到浏览器”按钮拆成两步，用户不清楚先后关系；本机路径和注册状态也与账号级偏好混在一起 | 复制协议注册命令后提供单一明确动作“我已执行命令，加载配置到浏览器”；点击时确认当前设备已执行命令、加载可共享的个人配置并保存当前设备的注册状态，随后显示“已确认执行”。SSH 与数据库客户端使用相同流程，修改客户端路径或注册命令后自动恢复为待确认状态。本机可执行文件路径、CA 路径、注册版本和配置指纹按设备保存，主题与终端字体等才作为账号级偏好同步 | 下次修改个人配置客户端注册流程时 | [当前客户端注册流程](../web/src/views/SettingsView.vue)、[当前浏览器配置缓存](../web/src/stores/preferences.ts) |
